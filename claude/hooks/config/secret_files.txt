# =============================================================================
# Secret Files Blacklist
# Files listed here are BLOCKED from being read by Claude Code.
# One entry per line. Lines starting with # are ignored.
# Entries are matched against the full file path (case-insensitive on Windows).
# You may use partial path suffixes, e.g.:
#   .env                  -> blocks any file named exactly ".env"
#   config/.env.local     -> blocks that specific relative path
#   /absolute/path/.env   -> blocks that exact absolute path
# =============================================================================

.env
